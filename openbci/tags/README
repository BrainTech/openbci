Wanna send tag?
- at the beginning of your file create TAGGER instance like:
  from tags import tagger
  TAGGER = tagger.get_tagger()

- set cursor in the line you want to send tag and type:
  TAGGER.send_tag(start_tag_timestamp, end_tag_timestamp, "tag_name", 
                                {other tag properties as dictionary})

- if your tag is an intermediate tag (start_tag_timestamp == end_tag_timestamp or the difference is irrelevant) fire:
  TAGGER.send_tag(start_tag_timestamp, start_tag_timestamp, ...)



Wanna receive tag? (see tags/tests/test_tags_receiving.py)
- create mx server, 
- register TAGS_RECEIVER peer
- create TAGGER instance as above
- in handle_message fire TAGGER.unpack_tag(mxmsg.message) 
- play with cool tag in dictionary format



Wanna know tag format?:
Dictionary with fields:
'start_timestamp' - float
'end_timestamp' - float
'name' - string
'desc' - dictionary